---
import Layout from '../layouts/Layout.astro';
import Icon from '../components/Icon.astro';
import ServiceCard from '../components/ServiceCard.astro';
import TeamCard from '../components/TeamCard.astro';
import SectionHeading from '../components/SectionHeading.astro';
import CtaBand from '../components/CtaBand.astro';
import TrustStrip from '../components/TrustStrip.astro';
import MapBlock from '../components/MapBlock.astro';
import StatsBand from '../components/StatsBand.astro';
import PressMarquee from '../components/PressMarquee.astro';
import Route66Shield from '../components/Route66Shield.astro';
import { site, telLink, telRendsburgLink, mailWorkshopLink, whatsappLink, fullServiceLeistungen } from '../data/site';
import { services, quickServiceTiles } from '../data/services';
import { team } from '../data/team';
import { reviewsMeta } from '../data/reviews';

const differentiators = [
  {
    num: '01',
    title: 'TV-Persönlichkeit. Echte Werkstatt.',
    body: 'Klaus Borrmann ist bekannt aus Cash für Chrom auf DMAX. Aber wenn die Kamera aus ist, steht er trotzdem in der Werkstatt - mit einem Schraubenschluessel in der Hand und V8-Oel an den Fingern.',
  },
  {
    num: '02',
    title: 'Komplette Import-Abwicklung.',
    body: 'Vom US-Verkaeufer bis zur deutschen Zulassung - wir übernehmen alles. Verhandlung, Logistik, Zollabwicklung, TÜV-Umruestung nach Paragraf 21 StVZO, deutsche Papiere. Du bekommst Dein US-Car schluesselfertig.',
  },
  {
    num: '03',
    title: '35+ Jahre Erfahrung, 4 Spezialisten.',
    body: 'Klaus, Michel (40 Jahre Ersatzteile), Fred (30 Jahre KFZ-Meister), Björn (V8 im Herzen, Rendsburg). Vier Charaktere, gemeinsam über 110 Jahre Erfahrung an amerikanischen Fahrzeugen.',
  },
];
---

<Layout
  title="US Cars | Route 66 Borrmann Motors KG | Hamburg Klaus Borrmann"
  description={site.description}
  current="/"
>
  <!-- HERO - light, with Ken-Burns + Route 66 Shield Watermark -->
  <section class="relative section-light pt-36 pb-20 md:pt-44 md:pb-28 overflow-hidden">
    <!-- Massive Route 66 Highway Shield Watermark -->
    <div class="absolute -right-32 top-20 hidden lg:block pointer-events-none" aria-hidden="true">
      <Route66Shield class="text-onyx" size={720} opacity={0.04} />
    </div>
    <!-- Small Route 66 number echo -->
    <span class="deco-number hidden xl:block" style="top: 32rem; left: 2rem; opacity: 0.03;">66</span>

    <div class="container-r66 relative z-10">
      <div class="grid lg:grid-cols-12 gap-10 lg:gap-14 items-center">
        <div class="lg:col-span-7 fade-in">
          <p class="eyebrow">US-Cars - KFZ-Meisterbetrieb - Hamburg seit 1989</p>
          <h1 class="heading-display text-balance">
            <span class="reveal-text block"><span class="reveal-text-inner">Dein US-Car.</span></span>
            <span class="reveal-text block"><span class="reveal-text-inner">Geliefert, gewartet,</span></span>
            <span class="reveal-text block"><span class="reveal-text-inner">restauriert <span class="text-route">in Hamburg.</span></span></span>
          </h1>
          <p class="mt-8 text-lg md:text-xl text-muted leading-relaxed max-w-2xl text-pretty">
            Klaus Borrmann und sein Team importieren, warten und restaurieren US-Cars seit 1989. Bekannt aus Cash für Chrom (DMAX). Official Shelby Service Partner. Werkstatt in Hamburg-Bahrenfeld, Zweigstelle Rendsburg.
          </p>
          <div class="mt-10 flex flex-wrap gap-3">
            <a href={site.external.autoscout24} target="_blank" rel="noopener" class="btn btn-primary btn-lg group">
              <Icon name="Car" size={16} strokeWidth={2} />
              Aktuelle Fahrzeuge ansehen
              <span class="hover-arrow inline-flex"><Icon name="ArrowRight" size={15} strokeWidth={2} /></span>
            </a>
            <a href={telLink} class="btn btn-outline btn-lg">
              <Icon name="Phone" size={16} strokeWidth={2} />
              {site.phoneDisplay}
            </a>
          </div>
          <div class="mt-12 pt-8 border-t border-line">
            <TrustStrip />
          </div>
        </div>
        <div class="lg:col-span-5 fade-in stagger-1">
          <div class="relative">
            <div class="rounded-xl overflow-hidden aspect-[4/5] bg-cream-warm">
              <img src="/images/klaus-borrmann.jpg" alt="Klaus Borrmann - Inhaber Route 66 Borrmann Motors" class="w-full h-full object-cover ken-burns" loading="eager" />
              <!-- Inhaber-Card jetzt OVERLAY innerhalb des Bildes statt floating -->
              <div class="absolute bottom-5 left-5 right-5 hidden sm:flex items-center gap-3 rounded-lg p-3.5 pr-5" style="background: rgba(255,254,251,0.96); backdrop-filter: blur(14px); border: 1px solid rgba(0,0,0,0.06); box-shadow: 0 18px 36px -12px rgba(0,0,0,0.35);">
                <div class="w-11 h-11 rounded-full overflow-hidden flex-shrink-0">
                  <img src="/images/klaus-borrmann.jpg" alt="" class="w-full h-full object-cover" />
                </div>
                <div class="flex-1 min-w-0">
                  <p class="text-[0.6rem] text-muted uppercase tracking-[0.16em] leading-none">Inhaber & KFZ-Meister</p>
                  <p class="font-display text-base font-medium text-onyx leading-tight mt-1">Klaus Borrmann</p>
                </div>
                <span class="inline-flex items-center gap-1 px-2 py-1 rounded-md bg-route text-white text-[0.62rem] font-medium tracking-wide flex-shrink-0">
                  <Icon name="Tv" size={10} strokeWidth={2.4} />
                  DMAX
                </span>
              </div>
            </div>
            <!-- Floating EST. 1989 badge top-right -->
            <div class="absolute -top-3 -right-3 hidden md:block">
              <div class="hero-badge !p-0 !rounded-full w-24 h-24 flex flex-col items-center justify-center text-center" style="background: #B91C1C; border-color: #991B1B;">
                <span class="font-display text-[0.6rem] text-white/85 uppercase tracking-widest">Est.</span>
                <span class="font-display text-2xl font-medium text-white leading-none">1989</span>
                <span class="font-display text-[0.55rem] text-white/85 uppercase tracking-widest mt-1">Hamburg</span>
              </div>
            </div>
            <!-- Floating Shelby Service Partner Badge -->
            <div class="absolute -left-4 top-1/2 -translate-y-1/2 hidden lg:flex rounded-lg p-3 items-center gap-2.5 max-w-[180px]" style="background: rgba(255,254,251,0.96); backdrop-filter: blur(14px); border: 1px solid rgba(0,0,0,0.06); box-shadow: 0 18px 36px -12px rgba(0,0,0,0.25);">
              <div class="w-10 h-10 rounded-md bg-onyx flex items-center justify-center flex-shrink-0">
                <img src="/images/shelby-circle-snake.png" alt="Shelby" class="w-7 h-7 object-contain" style="filter: brightness(1.1);" />
              </div>
              <div>
                <p class="text-[0.55rem] text-muted uppercase tracking-widest leading-none">Official</p>
                <p class="font-display text-xs font-medium text-onyx leading-tight mt-0.5">Shelby Service<br/>Partner</p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- Scroll hint -->
    <div class="hidden md:flex absolute bottom-6 left-1/2 -translate-x-1/2 flex-col items-center gap-2 text-muted text-[0.68rem] uppercase tracking-widest">
      <span>Scroll</span>
      <span class="block w-px h-8 bg-onyx/20"></span>
    </div>
  </section>

  <!-- PRESS MARQUEE -->
  <PressMarquee />

  <!-- AUTHORITY QUOTE -->
  <section class="section-light section-padding relative overflow-hidden">
    <Route66Shield class="absolute -left-16 top-1/2 -translate-y-1/2 text-onyx" size={420} opacity={0.05} />
    <div class="container-r66 relative">
      <div class="max-w-4xl mx-auto text-center fade-in">
        <Icon name="Quote" size={42} strokeWidth={1.5} class="text-route mx-auto mb-7" />
        <p class="font-display text-2xl md:text-4xl text-onyx leading-snug text-balance">
          Route 66 in Hamburg ist der erfolgreiche und aus Cash für Chrom bekannte Importeur, Tuner und Service-Partner für US-Fahrzeuge in <span class="brush-underline">ganz Deutschland</span>.
        </p>
        <a href={site.external.cashFürChrom} target="_blank" rel="noopener" class="arrow-link mt-10 inline-flex">
          Cash für Chrom auf DMAX <Icon name="ExternalLink" size={13} strokeWidth={2} />
        </a>
      </div>
    </div>
  </section>

  <!-- DREI USPs (premium cards mit images) -->
  <section class="section-warm section-padding relative overflow-hidden">
    <Route66Shield class="absolute -right-20 -bottom-20 text-route" size={460} opacity={0.05} />
    <div class="container-r66 relative">
      <div class="flex flex-wrap items-end justify-between gap-6 mb-16">
        <div class="max-w-xl fade-in">
          <p class="eyebrow">Warum Route 66</p>
          <h2 class="heading-xl text-balance">Was uns ausmacht.</h2>
        </div>
        <p class="max-w-md text-muted leading-relaxed fade-in stagger-1">Drei Dinge, die uns von der normalen Werkstatt um die Ecke unterscheiden. Und die uns seit 1989 zur ersten Adresse für US-Cars in ganz Deutschland gemacht haben.</p>
      </div>
      <div class="grid lg:grid-cols-3 gap-6 lg:gap-7">
        {differentiators.map((d, i) => {
          const dImages = ['/images/klaus-borrmann.jpg', '/images/team-header.jpg', '/images/team-group.jpg'];
          const dIcons = ['Tv', 'Ship', 'Users'];
          return (
            <div class={`usp-card fade-in stagger-${i + 1}`}>
              <div class="usp-media">
                <img src={dImages[i]} alt={d.title} loading="lazy" />
                <span class="usp-num">{d.num}</span>
                <span class="usp-icon">
                  <Icon name={dIcons[i]} size={18} strokeWidth={1.8} />
                </span>
              </div>
              <div class="usp-body">
                <h3 class="heading-md mb-3 leading-tight">{d.title}</h3>
                <p class="text-muted leading-relaxed text-sm">{d.body}</p>
              </div>
            </div>
          );
        })}
      </div>
    </div>
  </section>

  <!-- STATS BAND -->
  <StatsBand />

  <!-- FULL-SERVICE LEISTUNGEN - alternating image + text -->
  <section class="section-white section-padding">
    <div class="container-r66">
      <div class="grid lg:grid-cols-12 gap-12 lg:gap-16 items-center mb-24">
        <div class="lg:col-span-6 fade-in">
          <p class="eyebrow">Full-Service Meisterwerkstatt</p>
          <h2 class="heading-xl text-balance mb-6">Neun Leistungen, ein Standort.</h2>
          <p class="text-muted leading-relaxed mb-8 text-lg">
            Vom US-Car-Import über Restaurierung bis TÜV-Abnahme: in Hamburg-Bahrenfeld haben wir die Kompetenz, das Werkzeug und die Geduld, die ein amerikanisches Fahrzeug verdient.
          </p>
          <ul class="space-y-3.5">
            {fullServiceLeistungen.map((l, i) => (
              <li class="flex items-start gap-3 group">
                <span class="w-6 h-6 rounded-full bg-route/10 text-route inline-flex items-center justify-center flex-shrink-0 mt-0.5 group-hover:bg-route group-hover:text-white transition-colors">
                  <Icon name="Check" size={12} strokeWidth={2.5} />
                </span>
                <span class="text-onyx">{l}</span>
              </li>
            ))}
          </ul>
          <a href="/services" class="arrow-link mt-10 inline-flex">
            Alle Services <Icon name="ArrowRight" size={14} strokeWidth={2} class="hover-arrow" />
          </a>
        </div>
        <div class="lg:col-span-6 fade-in stagger-1">
          <div class="relative">
            <div class="rounded-xl overflow-hidden aspect-[4/3] bg-cream-warm">
              <img src="/images/team-group.jpg" alt="Route 66 Team in der Werkstatt Hamburg" class="w-full h-full object-cover" loading="lazy" />
            </div>
            <!-- floating "9 Leistungen" badge -->
            <div class="absolute -bottom-4 -right-4 bg-onyx rounded-lg px-5 py-4 shadow-xl">
              <p class="font-display text-3xl text-white leading-none">9</p>
              <p class="text-white/65 text-[0.7rem] uppercase tracking-widest mt-1">Leistungen</p>
            </div>
          </div>
        </div>
      </div>

      <!-- Reverse layout: Import expertise -->
      <div class="grid lg:grid-cols-12 gap-12 lg:gap-16 items-center">
        <div class="lg:col-span-6 lg:order-2 fade-in">
          <p class="eyebrow">Import-Expertise</p>
          <h2 class="heading-xl text-balance mb-6">Dein Traumwagen in sicheren Haenden.</h2>
          <p class="text-muted leading-relaxed mb-8 text-lg">
            Mit unserer Erfahrung im Fahrzeugimport garantieren wir eine schnelle und reibungslose Lieferung Deines Wunschfahrzeugs. Von der Zollabfertigung über die TÜV-Umruestung bis zu den deutschen Fahrzeugpapieren - wir übernehmen die komplette Abwicklung für Dich.
          </p>
          <div class="grid sm:grid-cols-2 gap-x-6 gap-y-4">
            {quickServiceTiles.map((t) => (
              <div class="flex items-start gap-3">
                <span class="w-9 h-9 rounded-md bg-route/8 text-route inline-flex items-center justify-center flex-shrink-0">
                  <Icon name={t.icon} size={16} strokeWidth={1.8} />
                </span>
                <div>
                  <p class="font-medium text-onyx text-sm leading-tight">{t.title}</p>
                  <p class="text-muted text-xs mt-1 leading-relaxed">{t.body}</p>
                </div>
              </div>
            ))}
          </div>
          <a href="/us-cars-kaufen" class="arrow-link mt-10 inline-flex">
            Wie der Import funktioniert <Icon name="ArrowRight" size={14} strokeWidth={2} class="hover-arrow" />
          </a>
        </div>
        <div class="lg:col-span-6 lg:order-1 fade-in stagger-1">
          <div class="relative">
            <div class="rounded-xl overflow-hidden aspect-[4/3] bg-cream-warm">
              <img src="/images/team-header.jpg" alt="US-Car Import - Klaus Borrmann" class="w-full h-full object-cover" loading="lazy" />
            </div>
            <!-- Route from US to Hamburg overlay badge -->
            <div class="absolute -top-4 -left-4 bg-white rounded-lg px-5 py-4 shadow-xl border border-line">
              <div class="flex items-center gap-3">
                <span class="font-display text-xs uppercase tracking-widest text-muted">USA</span>
                <Icon name="ArrowRight" size={14} strokeWidth={2} class="text-route" />
                <span class="font-display text-xs uppercase tracking-widest text-onyx font-medium">Hamburg</span>
              </div>
              <p class="text-xs text-muted mt-2">Komplette Import-Abwicklung</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- TEAM with hover-reveal -->
  <section class="section-light section-padding relative overflow-hidden">
    <span class="deco-number hidden lg:block" style="bottom: 5rem; left: -2rem; font-size: 14rem; opacity: 0.04;">TEAM</span>
    <div class="container-r66 relative">
      <div class="flex flex-wrap items-end justify-between gap-6 mb-16">
        <div class="max-w-2xl fade-in">
          <p class="eyebrow">Das Team</p>
          <h2 class="heading-xl text-balance">Vier Charaktere. 110+ Jahre Erfahrung.</h2>
        </div>
        <a href="/team" class="arrow-link fade-in stagger-1">Mehr über das Team <Icon name="ArrowRight" size={14} strokeWidth={2} class="hover-arrow" /></a>
      </div>

      <div class="grid sm:grid-cols-2 lg:grid-cols-4 gap-6 lg:gap-8">
        {team.map((m, i) => (
          <div class={`fade-in stagger-${i + 1}`}>
            <TeamCard member={m} index={i} />
          </div>
        ))}
      </div>

      <p class="text-center text-muted text-sm mt-12 fade-in">
        <span class="inline-flex items-center gap-2">
          <Icon name="Hand" size={14} strokeWidth={1.8} class="text-route" />
          Halte den Mauszeiger auf ein Portrait für die Geschichte.
        </span>
      </p>
    </div>
  </section>

  <!-- SERVICES GRID -->
  <section class="section-white section-padding">
    <div class="container-r66">
      <div class="flex flex-wrap items-end justify-between gap-6 mb-16">
        <div class="max-w-2xl fade-in">
          <p class="eyebrow">Services & Beratung</p>
          <h2 class="heading-xl text-balance">Vom Call a Mechanic bis Werkstattführung.</h2>
        </div>
        <a href="/services" class="arrow-link fade-in stagger-1">Alle Services <Icon name="ArrowRight" size={14} strokeWidth={2} class="hover-arrow" /></a>
      </div>

      <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
        {services.map((s, i) => (
          <div class={`fade-in stagger-${(i % 3) + 1}`}>
            <ServiceCard service={s} index={i} />
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- WERKSTATTFÜHRUNG TEASER -->
  <section class="section-warm section-padding relative overflow-hidden">
    <Route66Shield class="absolute -right-12 -top-12 text-route" size={380} opacity={0.06} />
    <div class="container-r66 relative">
      <div class="grid lg:grid-cols-12 gap-12 lg:gap-16 items-center">
        <div class="lg:col-span-5 fade-in">
          <div class="rounded-xl overflow-hidden aspect-[4/5] bg-cream-deep">
            <img src="/images/car-design-1.jpg" alt="Custom Build in der Werkstatt" class="w-full h-full object-cover ken-burns" loading="lazy" />
          </div>
        </div>
        <div class="lg:col-span-7 fade-in stagger-1">
          <span class="pill pill-red mb-6">
            <Icon name="Sparkles" size={12} strokeWidth={2} />
            Neu - ab Mai 2026
          </span>
          <h2 class="heading-xl text-balance mb-6">Private Werkstattführung.</h2>
          <p class="text-lg text-muted leading-relaxed mb-4 text-pretty">
            Ein Blick hinter die Kulissen - dort, wo Leidenschaft auf Motoren trifft.
          </p>
          <p class="text-muted leading-relaxed mb-8">
            Exklusive Führung durch Werkstatt und Ausstellungsräume. Persönliche Einblicke in Projekte. Zeit für Fragen. Kaffee und Wasser inklusive. Maximal 3 bis 4 Personen, ca. 45 Minuten.
          </p>

          <div class="flex flex-wrap gap-x-8 gap-y-3 mb-10 text-sm">
            <span class="inline-flex items-center gap-2 text-muted">
              <Icon name="Users" size={14} strokeWidth={2} class="text-route" />
              Max. 3-4 Personen
            </span>
            <span class="inline-flex items-center gap-2 text-muted">
              <Icon name="Clock" size={14} strokeWidth={2} class="text-route" />
              ca. 45 Minuten
            </span>
            <span class="inline-flex items-center gap-2 text-muted">
              <Icon name="Euro" size={14} strokeWidth={2} class="text-route" />
              300 EUR / Führung
            </span>
          </div>

          <div class="flex flex-wrap gap-3">
            <a href="/services/werkstattfuehrung" class="btn btn-primary btn-lg group">
              <Icon name="Compass" size={16} strokeWidth={2} />
              Werkstattführung buchen
              <span class="hover-arrow inline-flex"><Icon name="ArrowRight" size={15} strokeWidth={2} /></span>
            </a>
            <a href={telLink} class="btn btn-outline btn-lg">
              <Icon name="Phone" size={16} strokeWidth={2} />
              Fragen? Anrufen
            </a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- JOB BANNER -->
  <section class="section-light py-16">
    <div class="container-r66">
      <div class="rounded-lg border border-line bg-white p-8 md:p-10 flex flex-col md:flex-row items-start md:items-center gap-6 justify-between transition-shadow hover:shadow-xl">
        <div class="flex items-start gap-5">
          <span class="w-14 h-14 rounded-lg bg-route/8 text-route inline-flex items-center justify-center flex-shrink-0">
            <Icon name="Briefcase" size={22} strokeWidth={1.8} />
          </span>
          <div>
            <p class="eyebrow mb-2">Wir stellen ein per sofort</p>
            <h3 class="heading-md mb-2">KFZ-Mechaniker / Mechatroniker (m/w/d).</h3>
            <p class="text-muted text-sm">Unbefristet, Vollzeit, übertarifliche Bezahlung, Snap-on Tools.</p>
          </div>
        </div>
        <a href="/jobs" class="btn btn-dark flex-shrink-0 group">
          Stellenausschreibung
          <span class="hover-arrow inline-flex"><Icon name="ArrowRight" size={15} strokeWidth={2} /></span>
        </a>
      </div>
    </div>
  </section>

  <!-- STANDORTE -->
  <section class="section-white section-padding">
    <div class="container-r66">
      <div class="max-w-2xl mb-16 fade-in">
        <p class="eyebrow">Unsere Standorte</p>
        <h2 class="heading-xl text-balance">Hamburg HQ, Zweigstelle Rendsburg.</h2>
      </div>

      <div class="grid lg:grid-cols-2 gap-8">
        <div class="fade-in">
          <MapBlock
            title="Hamburg - Hauptstandort"
            embedUrl={site.googleMapsEmbed}
            address={`${site.address.street}, ${site.address.streetDetail}, ${site.address.postal} ${site.address.city}`}
            directionsUrl={site.googleDirectionsUrl}
            phone={site.phoneDisplay}
            phoneIntl={site.phoneIntl}
            email={site.email}
            hours={['Mo - Do: 8 - 17 Uhr', 'Fr: 8 - 15 Uhr', 'Sa: Nach Vereinbarung']}
          />
        </div>
        <div class="fade-in stagger-1">
          <MapBlock
            title="Rendsburg - geleitet von Björn"
            embedUrl={site.googleMapsEmbedRendsburg}
            address={`${site.addressRendsburg.street}, ${site.addressRendsburg.postal} ${site.addressRendsburg.city}`}
            directionsUrl={site.googleMapsUrlRendsburg}
            phone={site.phoneRendsburgDisplay}
            phoneIntl={site.phoneRendsburgIntl}
            email={site.emailRendsburg}
            hours={['Termine nach Absprache', 'V8 im Herzen']}
          />
        </div>
      </div>
    </div>
  </section>

  <!-- REVIEWS - dramatic with trust badges + big quote -->
  <section class="section-warm section-padding relative overflow-hidden">
    <Route66Shield class="absolute -left-32 top-1/2 -translate-y-1/2 text-onyx" size={520} opacity={0.04} />
    <div class="container-r66 relative">
      <div class="grid lg:grid-cols-12 gap-12 lg:gap-16 items-center">
        <div class="lg:col-span-5 fade-in">
          <p class="eyebrow">Vertrauen</p>
          <h2 class="heading-xl text-balance mb-7">35 Jahre. Tausende Kunden. Eine Reputation.</h2>
          <p class="text-muted leading-relaxed text-lg mb-8">
            Echte Bewertungen statt erfundener Testimonials. Wir verlinken direkt auf Google, AutoScout24 und Cash für Chrom - dort findest Du, was unsere Kunden tatsächlich sagen.
          </p>
          <a href={reviewsMeta.googleSearchUrl} target="_blank" rel="noopener" class="btn btn-primary btn-lg group">
            <Icon name="Star" size={16} strokeWidth={2} />
            Google-Bewertungen ansehen
            <span class="hover-arrow inline-flex"><Icon name="ArrowRight" size={15} strokeWidth={2} /></span>
          </a>
        </div>

        <div class="lg:col-span-7 fade-in stagger-1">
          <div class="grid sm:grid-cols-2 gap-5">
            <!-- Trust-Karte Google -->
            <div class="trust-card">
              <div class="flex items-start justify-between gap-3 mb-5">
                <div class="stars">
                  {Array.from({ length: 5 }).map(() => (
                    <svg width="18" height="18" viewBox="0 0 24 24"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
                  ))}
                </div>
                <span class="trust-source">Google</span>
              </div>
              <p class="font-display text-4xl text-onyx font-medium leading-none mb-2">4.9 ★</p>
              <p class="text-muted text-sm">Bewertungen von echten Kunden auf Google Maps</p>
              <a href={reviewsMeta.googleSearchUrl} target="_blank" rel="noopener" class="trust-link mt-5">Auf Google ansehen <Icon name="ExternalLink" size={11} strokeWidth={2} /></a>
            </div>

            <!-- Trust-Karte AutoScout24 -->
            <div class="trust-card">
              <div class="flex items-start justify-between gap-3 mb-5">
                <div class="stars">
                  {Array.from({ length: 5 }).map(() => (
                    <svg width="18" height="18" viewBox="0 0 24 24"><path d="M12 2l3.09 6.26L22 9.27l-5 4.87 1.18 6.88L12 17.77l-6.18 3.25L7 14.14 2 9.27l6.91-1.01L12 2z"/></svg>
                  ))}
                </div>
                <span class="trust-source">AutoScout24</span>
              </div>
              <p class="font-display text-4xl text-onyx font-medium leading-none mb-2">Verifiziert</p>
              <p class="text-muted text-sm">Verifizierter Händler-Status mit aktuellem Fahrzeug-Bestand</p>
              <a href={site.external.autoscout24} target="_blank" rel="noopener" class="trust-link mt-5">Profil ansehen <Icon name="ExternalLink" size={11} strokeWidth={2} /></a>
            </div>

            <!-- Trust-Karte Cash für Chrom -->
            <div class="trust-card sm:col-span-2 trust-card-feature">
              <div class="flex items-start gap-5">
                <div class="w-14 h-14 rounded-xl bg-route/10 text-route inline-flex items-center justify-center flex-shrink-0">
                  <Icon name="Tv" size={22} strokeWidth={1.8} />
                </div>
                <div class="flex-1">
                  <p class="text-[0.66rem] tracking-[0.16em] uppercase text-route font-medium mb-1">Bekannt aus</p>
                  <p class="font-display text-2xl text-onyx font-medium leading-tight mb-3">Cash für Chrom - DMAX</p>
                  <p class="text-muted text-sm leading-relaxed mb-5">Klaus Borrmann ist seit Jahren in der DMAX-Sendung "Cash für Chrom" zu sehen - die Show, die Schrottplätzen ihren wahren Wert gibt.</p>
                  <a href={site.external.cashFürChrom} target="_blank" rel="noopener" class="trust-link">Sendung auf DMAX <Icon name="ExternalLink" size={11} strokeWidth={2} /></a>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <CtaBand />
</Layout>
